$.modal.close();

$.modal("<%= escape_javascript(render :partial => 'end_rental') %>");



$(document).ready(function(){
Date.firstDayOfWeek = 0;
Date.format = 'yyyy-mm-dd';
$('#message_rental_end_date').datePicker({startDate:'<%= @location.renter_date %>',clickInput:true});

});
