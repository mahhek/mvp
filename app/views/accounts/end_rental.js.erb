$.modal.close();

$.modal("<%= escape_javascript(render :partial => 'end_rental') %>");



$(document).ready(function(){
Date.firstDayOfWeek = 0;
Date.format = 'yyyy-mm-dd';
$('#renting_end_date').datePicker({startDate:'<%= @request_for_location.buyer_rental_date %>',clickInput:true});

});
